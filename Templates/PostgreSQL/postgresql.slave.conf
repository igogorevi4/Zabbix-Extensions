#UserParameter = pgsql.slave.delay[*], psql -qAtX $1 -c "SELECT CASE WHEN pg_last_xlog_receive_location() = pg_last_xlog_replay_location() THEN 0 ELSE EXTRACT (EPOCH FROM now() - pg_last_xact_replay_timestamp()) END AS log_delay"       # up to v9.6
UserParameter = pgsql.slave.delay[*], psql -qAtX $1 -c "SELECT CASE WHEN pg_last_wal_receive_lsn() = pg_last_wal_replay_lsn() THEN 0 ELSE EXTRACT (EPOCH FROM now() - pg_last_xact_replay_timestamp()) END AS log_delay"                    # for v10+
UserParameter = pgsql.slave.runnig, ps aux | grep postgres | grep "wal receiver process" -c