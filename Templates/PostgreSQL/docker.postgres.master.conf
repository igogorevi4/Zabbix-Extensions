UserParameter=postgres.who.is.master, docker exec -t pgpool psql -h 127.0.0.1 -t -U postgres -d replication_db -c "SELECT name FROM repmgr_pg_cluster.repl_nodes WHERE type='master';" # | grep [a-z,0123456789]